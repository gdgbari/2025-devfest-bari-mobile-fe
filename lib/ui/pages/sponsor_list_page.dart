import 'package:flutter/material.dart';

class SponsorListPage extends StatelessWidget {
  const SponsorListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
